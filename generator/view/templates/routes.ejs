import { RouteRecordRaw } from 'vue-router'

const routes: Array<RouteRecordRaw> = [
  {
  name: '<%= routesName %>',
    path: '/<%= routesPath %>',
      component: () => import('<%= routesImportPath %>'),
        },
        ]

        export default routes